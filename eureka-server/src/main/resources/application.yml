spring:
  application:
    name: eureka-server
  security:
    basic:
      enabled: true
    user:
      name: ms-eureka-user
      password: senha-de-estudos

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
